fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
elasticsearch>=8
requests
python-dotenv
httpx
pytest
# Read-only in the test suite: tests/test_static_delivery.py opens the shipped
# woff2 faces to check their glyph tables. brotli is what decompresses woff2.
fonttools
brotli
# Drives a headless Chrome over the DevTools protocol, which is the only way to
# assert a computed style: Devanagari is unicameral, so no HTML-content check can
# see a text-transform or letter-spacing that reached it through the cascade.
websocket-client
